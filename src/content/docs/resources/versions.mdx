---
title: Versions
description: Current LightNet package versions these docs are verified against, plus tips for checking and updating your project.
---

import { Aside } from "@astrojs/starlight/components";

These docs are verified against the following package versions:

| Package                 | Version  |
| ----------------------- | -------- |
| `lightnet`              | `4.1.0`  |
| `@lightnet/sveltia-admin` | `4.0.0`  |
| `@lightnet/cli` | `4.1.0`  |

## Check your project

Run the following command in your project directory to see your installed versions:

```bash
npm ls lightnet @lightnet/sveltia-admin
```

If a package is missing from the tree, check `package.json` to see whether it is listed as a dependency.

## If your versions differ

- Minor and patch updates are generally safe. For guidance, see [Update your site](/run/update/).
- Major updates may include breaking changes. Review the [LightNet release notes](https://github.com/lightnetdev/lightnet/releases) before upgrading.

<Aside>
  LightNet follows Semantic Versioning. You can usually update to the latest
  minor and patch release without breaking your site, but major releases may
  require changes to your project.
</Aside>

## When reporting issues

Include your `lightnet` and `@lightnet/sveltia-admin` versions from `npm ls` so we can help more quickly.
