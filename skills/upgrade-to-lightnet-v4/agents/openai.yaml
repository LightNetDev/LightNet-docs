interface:
  display_name: "Upgrade to LightNet v4"
  short_description: "Upgrade downstream LightNet sites"
  default_prompt: "Use $upgrade-to-lightnet-v4 to audit a downstream LightNet site repo, run the safe v4 migration scripts, complete the manual changes, and verify the upgrade."
